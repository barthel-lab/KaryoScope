"""KaryoScope subcommands.

Each module in this package contributes one click command, exported as
``cmd``. The :mod:`karyoscope.cli` module aggregates them into the
top-level ``karyoscope`` group.
"""
